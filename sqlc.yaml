version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/repo/queries"
    schema: "migrations"
    gen:
      go:
        package: "repo"
        out: "internal/repo"
        emit_interface: true
        emit_db_tags: true
        emit_json_tags: true
        json_tags_case_style: "snake"
